import 'package:flutter/material.dart';

class PhotoTabFeature extends StatelessWidget {
  const PhotoTabFeature({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.yellow,
      child: const Center(
        child: Text('This is the photo tab'),
      ),
    );
  }
}
