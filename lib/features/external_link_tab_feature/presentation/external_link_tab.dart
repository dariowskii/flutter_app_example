import 'package:flutter/material.dart';

class ExternalLinkTab extends StatelessWidget {
  const ExternalLinkTab({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.red,
      child: const Center(
        child: Text('This is the external link tab'),
      ),
    );
  }
}
